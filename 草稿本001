是的，我也判断：之前的方向被你们 CMDB 平台机制坑住了。不是你操作问题。
从 v5 日志看，脚本已经确实换成了最新版：
Wrapper version: v5_force_patrol_home_venv_20260509
但变量最后还是变成：
/usr/bin/python3.10
这说明你们 CMDB 平台可能会对包含 python 的变量名做特殊注入/覆盖，甚至影响脚本内部变量。继续围绕 venv_python / ceph_venv_python / ceph_python 修，性价比很低。
我换了一个思路，做了 v6 版本：
下载 v6 脚本
下载 v6 打包版
v6 的核心变化
这版彻底不再使用这些名字：
venv_pythonceph_venv_pythonceph_python
也不再读取任何带 python 的 CMDB 输入参数。
它只根据：
patrol_home
自动拼出固定路径：
/root/ceph_patrol/v5_offline_install/ceph_patrol/venv/bin/python3
并且内部变量名改成：
runtime_bin
输出字段也改成：
runtime_bin_used
这样避开你们 CMDB 对 python 类字段的特殊处理。
你这次 CMDB 输入参数只保留这些
不要再填 venv_python，也不要填 ceph_venv_python。
dry_run              truemail_subject         【EasyOps】Ceph自动化巡检报告output_dir           /root/ceph_patrol/v5_offline_install/ceph_patrol/outputpatrol_home          /root/ceph_patrol/v5_offline_install/ceph_patrolpatrol_timeout       10receiver_emails      sz.zzli@qevoc.comsender_email         sz.yys9@qevoc.comsmtp_password        ************smtp_port            25smtp_server          mail.qevoc.comsmtp_username        sz.yys9@qevoc.com
正确日志应该长这样
第一行版本号：
Wrapper version: v6_runtime_bin_no_python_variable_20260509
然后关键看这个输出：
CMDB_OUTPUT runtime_bin_used=/root/ceph_patrol/v5_offline_install/ceph_patrol/venv/bin/python3
以及启动命令：
Start ceph patrol command: /root/ceph_patrol/v5_offline_install/ceph_patrol/venv/bin/python3 /root/ceph_patrol/v5_offline_install/ceph_patrol/ceph_patrol.py ...
只要这里变成 venv/bin/python3，这个老问题就结束了。
更稳的最终方案
如果 v6 还被 CMDB 干扰，我建议就彻底不要在 CMDB 里写完整 Python 逻辑了，改成：
CMDB 只负责触发一个服务器本地 shell 脚本真正巡检和发邮件逻辑放在 /root/ceph_patrol/... 目录里的脚本中
也就是 CMDB 里面只写一行：
/bin/bash /root/ceph_patrol/v5_offline_install/ceph_patrol/run_ceph_patrol_cmdb.sh
这样就绕开 CMDB 对 Python 变量的注入机制。当前先试 v6；如果 v6 仍异常，就直接切 shell 外层包装方案。
